spring:
  application:
    name: typeahead-backend

  datasource:
    url: jdbc:mysql://localhost:3306/typeahead_db
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true